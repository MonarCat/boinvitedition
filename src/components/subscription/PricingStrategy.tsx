import React from 'react';

export const PricingStrategy: React.FC = () => {
  return (
    <div>
      {/* The old pricing strategy component has been removed. */}
      <p>We have simplified our pricing. We now offer a simple Pay As You Go model.</p>
    </div>
  );
};
